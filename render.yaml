services:
  - type: web
    name: water-pipeline
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
    disk:
      name: pipeline-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
